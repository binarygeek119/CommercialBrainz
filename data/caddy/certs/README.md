# Cloudflare Origin CA files for Caddy (`CADDY_TLS_MODE=origin`)
#
# Place here (on the VM):
#   origin.crt  — Origin Certificate from Cloudflare
#   origin.key  — Private Key
#
# Mounted read-only into Caddy at /etc/caddy/certs/
# See docs/cloudflare-domain.md
