# VM override: expose web on port 80, keep DB/Redis internal-only
services:
  postgres:
    ports: []

  redis:
    ports: []

  web:
    ports:
      - "80:80"
    depends_on:
      api:
        condition: service_healthy

  api:
    ports:
      - "8000:8000"
